import { Meta, StoryObj } from '@storybook/react'
import { Loading } from './Loading'

export default {
  title: 'Widget UI/Loading',
  component: Loading
} as Meta

export const Default: StoryObj = {}
